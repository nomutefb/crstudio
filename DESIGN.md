# DESIGN.md — 디자인 기틀 (항상 이 문서를 따른다)

이 사이트의 모든 페이지·컴포넌트·수정 작업은 아래 원칙을 따른다.
**원칙에 없는 값·스타일을 임의로 추가하지 않는다.** 새 값이 필요하면 먼저 이 문서와 `assets/css/tokens.css`에 추가한 뒤 사용한다.

---

## 1. 아이덴티티 — "Paper & Ink"

- 종이(#f4f1ea) 위에 잉크(#141311). **모노크롬 고정.**
- 컬러는 오직 콘텐츠(작품·사진)만 가진다. UI에 유채색 금지.
- 질감: 전면 필름 그레인 오버레이(5.5%), 히어로의 잉크 스모크 드리프트. 그 외 장식 금지.
- 무드: 미술관의 절제 + 포스터의 대담함. 요소 수는 줄이고 크기 대비는 키운다.

## 2. 타이포그래피

| 역할 | 폰트 | 규칙 |
|---|---|---|
| 디스플레이(제목·이름) | **Anton** | 항상 uppercase, line-height 0.92~1.05 |
| 국문 디스플레이 폴백 | Pretendard(굵게) | 영문 표기 없을 때 자동 폴백 |
| 라벨·내비 | **Jost** | 11px, uppercase, letter-spacing 0.22~0.32em |
| 본문 | **Pretendard** | 15px, line-height 1.75, `word-break: keep-all` |

- 크기는 `tokens.css`의 `--fs-hero`, `--fs-display-1~5`만 사용. **새 크기 생성 금지.**
- 서체 추가 금지. 3서체 체계를 유지한다.

## 3. 색·선·면

- 색은 `--paper --paper-2 --ink --ink-soft --muted --dark` 6개가 전부.
- 구분선은 1px 헤어라인(`--line`), 강조 보더만 `--ink`.
- 그림자 사용 금지. 깊이는 색 대비와 겹침으로만 표현.
- 라운드는 두 가지만: 태그·원형 버튼 `--radius-pill`, 인용 카드 `--radius-card`.

## 4. 레이아웃

- 좌우 여백 `--pad`, 섹션 상하 `--sect`. 임의 마진 금지.
- 섹션 헤더 패턴 고정: 좌측 대형 타이틀 + 우측 라벨/링크 (baseline 정렬).
- 리스트는 풀블리드 헤어라인 행(작가), 그리드는 3열→2열→1열로 붕괴.
- 이미지 비율 고정: 카드 4:5, 커버 21:9, 캐러셀 3:2. 임의 비율 금지.

## 4.5 홈 = 잡지(Book) 모드

- 데스크톱(≥901px) 홈은 한 화면 = 한 페이지. **휠 제스처 1회 = 1페이지 플립(0.9s power2.inOut)** — 페이지 안에서 부분 스크롤 금지. 제스처 판정은 90ms 이벤트 갭으로만 하고, 애니메이션 종료 후 새 플릭에는 즉시 반응해야 한다(잠금·쿨다운으로 "힘줘야 넘어가는" 느낌 금지).
- 연례 간트 알약은 잉크 채움(#141311) + 종이색 볼드 텍스트, 연도 트랙 높이는 전부 동일(3단 기준 104px).
- 페이지 구성: 커버(스튜디오 사진 무채색 오버랩) → 현재 기수 작가 → 포스터 벨트 → 백커버.
- 홈에는 작가와 전시만 둔다. 연례 간트·레지던시 통계처럼 찾아보는 정보는 홈에서 빼고 Program 페이지(메뉴)로 보낸다.
- 사진은 항상 4:5 비율 우선. 화면이 낮아 잘라야 하면 **상단(얼굴) 기준**으로만 자른다.
- 포스터 벨트: 3:4 포스터 카드, 첫 노출 2초 후 우→좌 38px/s 무한 컨베이어, 호버 시 정지.
- 연례 간트: 좌측 연도·기수·작가, 우측 1–12월 스케일 위에 기간 알약. 월 미상 행사는 트랙 아래 작은 칩.

## 5. 모션 — "느리게 등장, 빠르게 반응"

- **등장(리빌)**: y +34px → 0, 1.0~1.15s, `power3/power4.out`, 스태거 0.07s, 1회만.
- **반응(호버·탭)**: 0.25s. 색 반전(잉크↔종이) 또는 아웃라인 전환으로만.
- **스크롤**: Lenis 스무스 스크롤(lerp 0.1). 커버 이미지는 ±7% 패럴랙스(scrub).
- **숫자**: 다크 밴드 진입 시 카운트업 1.6s.
- **금지**: bounce·elastic 이징, 무한 반복 장식 애니메이션(마퀴·스모크 제외), 0.25s보다 빠른 감속 없는 컷.
- `prefers-reduced-motion` 시 모든 모션 해제 — 필수.

## 6. 컴포넌트 어휘 (이것만 조합해서 만든다)

`label`(라벨) · `display`(대형 타이틀) · `btn`(아웃라인 버튼) · `cohort-tab`(필터 필)
· `artist-row`(대형 이름 행 + 호버 플로팅 이미지) · `post-card`(4:5 카드)
· `project-block`(21:9 에디토리얼 블록) · `band`(다크 통계 밴드) · `quote-card`
· `acc`(아코디언) · `pn`(prev/next 내비) · `marquee` · `tide`(우측 하단 물때 내비)

## 6.5 물때 내비 (`tide`)

- 우측 하단 고정. 여백은 `--float-gap`, 레이어는 `--z-float`(헤더보다 아래).
- 버튼은 알약 아웃라인 + 픽토그램. 호버·열림 시 잉크↔종이 반전만 한다.
- `<details>`로 만든다 — JS 없이도 열리고 닫혀야 한다. JS는 갱신·바깥 클릭 닫기만 얹는다.
- 패널 안의 시간표는 예울마루가 제공하는 외부 SVG다. **UI 무채색 규칙의 예외는 이 이미지 하나뿐**이며,
  이미지 밖 테두리·라벨·문구는 전부 페이퍼&잉크를 지킨다. 못 불러오면 링크 안내로 대체한다.
- 주소·문구는 `_data/settings.yml`의 `tide:`에서만 바꾼다(관리자 페이지에 노출됨).

## 7. 콘텐츠 톤

- 섹션 타이틀은 영어 한 단어(ARTISTS, PROJECTS…) + 라벨에 국문 병기.
- 작가가 주인공: 이름이 화면에서 가장 크다. UI가 작품보다 화려해지면 실패.
- 자리표시 이미지는 `assets/img/ph/` 6종만 사용(모노크롬 기하 컴포지션).

## 8. 기술 규칙

- 외부 CDN 의존 금지 — 폰트·라이브러리는 전부 레포에 자체 호스팅.
- 라이브러리는 Lenis(스크롤), GSAP+ScrollTrigger(모션)만. 추가 금지.
- JS 없이도 모든 콘텐츠가 보여야 한다(`html.js` 게이트, IO 폴백).
- 값 하드코딩 금지 — `tokens.css` 변수만 사용.
